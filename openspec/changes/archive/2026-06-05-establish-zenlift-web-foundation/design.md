# Design

This change removes public template identity without changing the full product narrative yet. Metadata describes Zenlift as a workout tracker. Navigation supports the later one-page landing sections: Features, Progress, and Routines. The footer uses Zenlift identity and keeps only real or deliberately prepared links.

CTA links use `#` until the real Google Play URL is available. This keeps the UI ready while avoiding a fabricated store listing.
