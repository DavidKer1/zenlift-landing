# Design

`public/zenlift.png` is the source asset for the web brand system. Derived PNG icons keep the source mark centered and legible at 32px and 180px sizes. The visible navbar and footer brand use the image plus the wordmark text `Zenlift`.

The SVG icon should represent the same mark style as the PNG so browsers that prefer SVG still receive a Zenlift asset rather than a placeholder.
